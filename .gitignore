# Secrets — never commit
.env

# Generated at runtime — no need to track
raw-feed.json
digest-ready.json
digest-output.md

# State & cache — personal to your machine
state-feed.json
youtube-handle-cache.json

# Node
scripts/node_modules/
node_modules/
npm-debug.log*

# macOS
.DS_Store
